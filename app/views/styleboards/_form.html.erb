<%= form_for(@styleboard) do |f| %>
  <% if @styleboard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@styleboard.errors.count, "error") %> prohibited this styleboard from being saved:</h2>

      <ul>
      <% @styleboard.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="field">
    <%= f.label :place %><br>
    <%= f.number_field :place %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
